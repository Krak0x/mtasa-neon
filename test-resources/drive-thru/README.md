# Drive-Thru story conformance resource

This resource targets the complete multiplayer-visible path of `SCRIPT_NAME SWEET3` from the installed GTA:SA `main.scm`. It plays native file cutscenes `SWEET2A` and `SWEET2B`, reconstructs the mission world, runs the Ballas chase, returns Sweet and Ryder to Grove Street, takes Smoke home, and presents the visible mission reward and passed tune. The coverage audit below is authoritative for remaining parity gaps; a successful happy-path run alone does not establish `1:1` parity.

## Commands

- `/drivethru` starts the mission from the native `SWEET2A` file cutscene.
- `/drivethrutracechat [on|off]` toggles the passive checkpoint trace in chat. It only presents observations already published by the mission and never changes server state.
- `/drivethruskip` requests the server-authorized native cutscene skip. The leader's original GTA skip input is also consumed through the same server path.
- `/drivethruabort` destroys mission entities and restores the leader's original position, dimension, appearance, health, armour, and weapons.
- `/drivethrusimfar` moves the occupied Greenwood more than three kilometres away during the active chase. It explicitly tests the managed Voodoo route and server-side Grove failure condition.

## SCM coverage audit

The installed `main.scm` has SHA-256
`601def3baae766ce6a23e2f0b9b48f6b33c9a64e2fc32eb4f22ddea8b868b0fa`.
The decompiled navigation block spans `SCRIPT_NAME SWEET3` at source lines
`70236..72673`. Decompiled source is not bytecode proof: the generated
`SET_TIME_SCALE 0.75` line in the Grove failure scene was disproved by the raw
mission bytecode and is intentionally absent from Neon.

Status meanings are `implemented`, `adapted`, `excluded`, `missing`, and
`uncertain`. Runtime evidence names only tests already performed by the user.

| SCM block | Entry and phase | Vanilla operation and exact parameters | Native or multiplayer dependency | Neon implementation and evidence | Status |
| --- | --- | --- | --- | --- | --- |
| `mission_start_sweet3` initialization, lines `70527..70579` | Mission entry | Register mission, initialize local state, load GXT `SWEET3` | Server-authoritative state machine; campaign registration is separate | Lua mission state and exact GXT block are implemented; campaign registration is in the exclusion ledger | adapted |
| Initial Greenwood and `SWEET2A`, lines `70580..70620` | Intro before player control | GREENWOO at `(2508.7,-1671.7,12.3)`, heading `166.9`, colors `59/34`, plate `GROVE4L`, fire proof, tyres protected, then native cutscene | GTA vehicle base-Z conversion and managed file-cutscene lifecycle | Exact visible vehicle properties and native cutscene validated; synchronized creation is deferred until cutscene teardown to avoid GTA invalidating the MTA vehicle | adapted |
| Intro crew creation, lines `70621..70720` | After `SWEET2A` | Smoke, Sweet, Ryder models, positions, headings, anim groups, TEC9, health `500`, protection flags, empty decision makers, passenger tasks `1/2/0` | SCM passenger index maps to MTA seat plus one; actor policies must survive streaming | Seats `2/3/1`, appearance, weapons, health and available policies validated. Three `DM_PED_MISSION_EMPTY` assignments remain unavailable | uncertain |
| CJ restore and first line, lines `70721..70786` | Intro world release | CJ position `(2516.54,-1671.20,12.87)`, heading `81.20`, camera restore, `SWE2_AA`, then `TWAR2_A` | Native audio completion and authoritative passenger occupancy are separate gates | Implemented and intro checkpoint validated | implemented |
| Drive to Cluckin' Bell, lines `70787..70940` | Outward drive | Car and coordinate blip swap; target box `(2404.1,-1891.5,12.3)` radius `4/4/4`; CJ seated; four wheels; `SWE2_BA..BM`; exit reminder cycle; `250` health branch | Native important-area rendering, grounded-car observation, conversation interruption | Navigation, audio, reminders, grounded arrival and scoped death/health monitoring implemented; happy path validated | implemented |
| Restaurant transition and `SWEET2B`, lines `70941..71028` | Arrival gate | Fixed camera, 1 s fade out, stage CJ, delete crew and Greenwood, directional load, cutscene safety, `SWITCH_STREAMING OFF`, native `SWEET2B` | Camera lease, streaming ownership and native cutscene teardown | Implemented and native completion validated | implemented |
| Greenwood and crew reconstruction, lines `71029..71104` | Black screen after `SWEET2B` | Greenwood `(2396.36,-1917.96,12.38)`, heading `267.3`; CJ driver; direct passenger slots Smoke `2`, Sweet `1`, Ryder `0` | Direct `CREATE_CHAR_AS_PASSENGER` slots differ from the intro task indices | Reconstruction uses MTA seats Smoke `3`, Sweet `2`, Ryder `1` independently from the intro task seats; the integrated off-stream run observed all three exact seats | implemented |
| Voodoo creation and properties, lines `71105..71128` | Restaurant rebuild | VOODOO `(2411.3098,-1928.8369,12.3906)`, colors `22/22`, tyres protected, load-collision flag `FALSE`, health `2700`, lock mode `3`, heading `178.7106`, threat blip | Opcode `0587` sets physical mask `0x4000` and enables mission-car ghost physics when world collision is absent | The managed assignment applies `setVehicleLoadCollisionFlag(voodoo, false)` before native task construction; the integrated route remained at road-height Z through all eight points | implemented |
| Eight-point Voodoo route, lines `71052..71062`, assignment at `71135` | Black pursuit barrier | Eight exact `TASK_CAR_DRIVE_TO_COORD` children, speeds `13,15,18,18,20,20,20,15`, normal mode, model `412`, avoid-cars | Native sequence activation, persistent sync owner, streaming lease, unloaded collision | The stable `native-task-runtime` handle leases driver and Voodoo, reports logical indices and transfers ownership before replacement tasks. Isolated migration tests and the complete `/drivethrusimfar` route through index `7` passed | implemented |
| Ballas driver and passenger, lines `71121..71148` | Pursuit construction | BALLAS2, TEC9, current weapon, accuracy `40`; passenger slot `0`; drive-by at Greenwood, range `5000`, RHS, frequency `60` | Mission actor policy and native drive-by targeting | Implemented; task activation, real shots and target damage validated | implemented |
| Grove support actors, lines `71149..71168` | Pursuit construction | FAM2/FAM3 at exact coordinates, health `30`, TEC9, stay in place, never targeted, paired chat | Two independent actor flags are not covered by protagonist protection | Actors, health, weapons, scalar persistent flags and paired chat are implemented; the remote-scene barrier verifies both actors before reveal | implemented |
| Ryder and Sweet drive-bys, lines `71169..71191` | Pursuit construction | Greenwood health `3100`; Ryder RHS `99`; Sweet LHS `99`; both target Voodoo | Native AI animation, aim, fire, damage and seat geometry | Implemented and validated after native AI and drive-by animation corrections | implemented |
| Chase loop and dialogue, lines `71217..71624` | Both Ballas alive or one surviving | Voodoo objective, help timing, `SWE2_CA/CC`, `GA..HE`, damage-trigger groups `DA..DD` then `EA..EE`, exit reminders | Conversation state must pause and resume; health deltas are server authoritative | Implemented; happy chase validated, reminder path implemented after audit | implemented |
| Voodoo disabled or one Ballas dies, lines `71377..71453`, `drive_by_the_ballers` | Chase transition | Remove vehicle blip, Ballas `TASK_KILL_CHAR_ON_FOOT` against CJ, Ryder/Sweet retarget surviving Ballas at frequency `70`, objective `K_BALLA` | GTA kill-on-foot may internally leave the vehicle or attack CJ's car depending on CJ state | Implemented and observed in game; one Ballas engaged while another died in the explosion | implemented |
| Greenwood low-health chase failure, lines `71516..71587` | Chase, Greenwood at or below `250` | `SWE2_KA`, immediate leave plus smart flee `100.0/10000` for crew, then `SWE3_D` and failure | Native leave/flee task activation plus natural warning completion | Implemented and built; dedicated in-game validation still pending | uncertain |
| Grove alternate failure scene, lines `71229..71376` | Voodoo reaches `(2502.89,-1674.71,12.37)` radius `8/8/5`, both Ballas alive | Fade, freeze player, area/collision loads, fixed camera, speed `1` straight-line drive, coordinate drive-by, accuracy `50`, shoot rate `90`, scripted deaths at `2000/2500`, speed `30` escape, teardown, `TW2_Y` | Raw bytecode overrides the false decompile time-scale line; native deaths need explicit observation | The final off-stream regression observed the two-phase route handoff, stable five-entity scene barrier, slow drive and coordinate drive-by, both scripted deaths, escape task, camera restoration and expected failure | implemented |
| Chase success transition, `return_to_the_hood` | Both Ballas dead | Clear Sweet/Ryder tasks, remove enemy blips, select Grove return target, clear chase audio | Stage-scoped failure monitors must change here | Implemented; chase completion validated | implemented |
| Grove return drive, lines `71625..71820` | Ballas dead, full crew retained | Target `(2513.3,-1671.9,12.52)` radius `4/4/4`, grounded and CJ seated, `SWE2_MA..MJ`, exit reminders, `SWE2_KB` low-health failure | Important-area visual and exact monitor lifetime | Implemented; happy return validated; low-health branch pending dedicated test | uncertain |
| Grove drop-off scene, lines `71821..71918` | Grove gate passed | Fixed camera, dialogue `SWE2_NA..NH`, CJ look-at Ryder, 17 s camera move after index `3`, Sweet then Ryder leave and walk, 500 ms offset, 6 s wait, native skip | Camera acceptance, observed movement, native sequence progress and skip cleanup | Implemented and happy path validated | implemented |
| Smoke return drive, lines `71919..72038` | Sweet and Ryder deleted | Target `(2066.4648,-1695.4436,12.5547)` radius `4/3.5/4`, `SWE2_OA..OJ`, Smoke reminder cycle, `SWE2_KB` low-health branch | Failure monitoring now covers Smoke and Greenwood only | Implemented; happy path validated; low-health branch pending dedicated test | uncertain |
| Smoke drop-off scene, lines `72039..72125` | Smoke gate passed | Vehicle camera mode `ZOOM_TWO`, 17 s move/track, 500 ms lead, CJ look-at Smoke, `SWE2_PA..PE`, leave and walk, 5 s wait, native skip | Camera state and native departure task must be observed separately | Implemented and happy path validated | implemented |
| Audio helpers and speaker mapping, lines `72194..72429` | Every driving and return conversation | 200 ms inter-line gate, native load/play/finish, subtitle print, facial talk and scripted-speech mute for exact speaker | Audio completion must clear subtitle and facial state | Mission dialogue path implemented and happy path validated. Native file-cutscene subtitles are a separate external dependency and remain unverified | implemented |
| Return-to-car helpers, lines `72430..72630` | CJ exits Greenwood | Five Smoke lines or seven crew lines; interrupt active channel; resume at next conversation index | Natural audio completion and actor/vehicle survival | Implemented after parity audit; not every reminder variant has been individually exercised | uncertain |
| `mission_sweet3_failed`, lines `72127..72136` | Any monitored failure or death/arrest | Restore time scale and print `M_FAIL` | Failure reason text is printed before common failure | Implemented for mapped mission branches; death/arrest integration follows MTA lifecycle | adapted |
| `mission_sweet3_passed`, lines `72138..72151` | Smoke scene complete | `M_PASSS`, `$200`, tune `1`, respect `5`, wanted clear, mission counters and progress stats | Visible reward separates from campaign persistence | Money, passed text and tune implemented; campaign operations excluded | adapted |
| `mission_cleanup_sweet3`, lines `72153..72191` | Success, failure or abort | Remove crew/blips, restore ambient systems, tyre policy, models, generator, audio and mission state; leave CJ with Greenwood after success | Isolated harness cleanup intentionally restores the pre-test snapshot after a delay | Entity/audio/camera cleanup is implemented. Post-success world state is a deliberate harness adaptation, not vanilla visual parity | adapted |
| External cutscene subtitle cues | During `SWEET2A` and `SWEET2B` | Possible vanilla file-cutscene text cues associated with external cutscene data | Managed MTA cutscene path does not currently present them in observed runs | User observed no subtitles during Drive-Thru or Tagging Up Turf file cutscenes; the vanilla cue source and native presentation path have not yet been established | uncertain |

### Immediate findings from the matrix

1. The corrected post-`SWEET2B` seats, managed Voodoo route and remote Grove
   failure scene passed their integrated `/drivethrusimfar` regression. The
   route reached logical index `7` more than three kilometres from the player,
   then transferred both persistent syncers before camera preparation.
2. `DM_PED_MISSION_EMPTY` remains an explicit missing primitive. Native
   file-cutscene subtitles remain an unresolved
   oracle and integration question rather than a proven missing implementation.
3. Voodoo health is set to vanilla `2700`, but the reported faster destruction
   remains a runtime uncertainty until shot damage and health history are
   measured in one controlled chase.
4. Successful harness cleanup restores the caller's previous world state rather
   than leaving CJ in the Greenwood outside Smoke's house. This is intentional
   test-resource behavior, not vanilla post-mission parity.

### Scope and exclusion ledger

| SCM operation | Classification | Reason | Visible consequence checked | Revisit trigger |
| --- | --- | --- | --- | --- |
| Mission-given/passed registration, respect, Sweet counter, passed stat and progress | excluded solo bookkeeping | Campaign save progression is outside the current multiplayer-visible scope | Passed message, `$200`, and tune remain implemented separately | Persistent campaign runtime is introduced |
| `CLEAR_WANTED_LEVEL` | excluded solo bookkeeping | The isolated mission dimension has no ordinary police response and the handoff excludes wanted cleanup | No wanted-dependent mission branch exists in SWEET3 | Missions share the ordinary wanted system |
| `CHECKPOINT_SAVE 99` | excluded solo bookkeeping | Mobile checkpoint persistence is not multiplayer mission behavior | No visible immediate effect in the mission loop | Save/resume support is introduced |
| Car generator, ambient-crime and density toggles | adapted multiplayer | The resource owns an isolated dimension rather than mutating global traffic for all players | Mission actors and cutscene areas are deterministic inside the dimension | Story runtime moves into the shared world |
| Ped relationships | adapted multiplayer | Explicit native tasks control every mission combatant and no ambient gangs exist in the isolated dimension | Chase targeting and real damage were observed | Ambient mission populations are enabled |
| `CLEAR_AREA`, model request/release and special-character slots | adapted multiplayer | MTA element ownership and managed cutscenes replace global SCM streaming bookkeeping | Required models and entities are validated at client barriers | Shared-world streaming or native special slots become authoritative |
| Active PS2 keyboard shortcut to mission pass | excluded developer shortcut | It bypasses mission gameplay and is unsuitable for the runtime | `/drivethruskip` is limited to cutscene skip, not mission completion | Never, unless an explicit developer-only harness command is requested |
| Commented chicken-shop civilians and commented task variants | excluded unreachable source | They are not executable in the installed mission | Raw mission control flow does not reach them | A different SCM build enables them |
| Generated `SET_TIME_SCALE 0.75` line | excluded decompile error | The installed raw SWEET3 bytecode proceeds directly to `TIMERA = 0` | Target bytecode was inspected and Neon leaves game speed unchanged | Target `main.scm` hash changes |
| Post-success snapshot restoration | adapted harness lifecycle | Prevents an isolated conformance run from leaking position, equipment or entities | Cleanup restores the caller deterministically | Drive-Thru moves from test resource to persistent story runtime |

## Implemented path and known deviations

- The mission acquires GXT block `SWEET3`. Despite the dialogue keys using the `SWE2_` prefix, the installed SCM does not load block `SWEET2` for Drive-Thru.
- The leader receives fresh-game CJ model and clothes before `SWEET2A`; cleanup restores the previous appearance.
- The Greenwood is created under the post-cutscene black screen at the installed coordinates with plate `GROVE4L`, palette colours 59 and 34 converted to light crystal blue and arctic white RGB entries, unlocked doors, Bounce FM, and the native tyre-only no-burst policy. Deferring the synchronized element until native cutscene teardown is invisible to players and prevents GTA from invalidating MTA's vehicle instance.
- Smoke, Sweet, and Ryder are created after the cutscene with the SCM `CREATE_CHAR` Z conversion, their motion groups, TEC9, 500 health, mission-actor classification, story protection, and native passenger-entry tasks. SCM passenger indices `1`, `2`, and `0` map to MTA seats `2`, `3`, and `1` respectively. Task dispatch waits for the Greenwood and every actor to be streamed, syncer-owned, policy-ready, and position-stable. Mission progression separately waits for all three authoritative seat transitions.
- `SWE2_AA` loads and finishes before `TWAR2_A` is printed. The driving conversation preserves events `37601..37612`, keys `SWE2_BA..SWE2_BM`, the original speakers, native facial talk, four-second initial delay, and 200 ms inter-line gate.
- Navigation swaps from the friendly blue car blip to the default cream destination blip when the leader drives. Leaving the car restores the car blip and `TW2_X` instruction.
- The terminal checkpoint requires the server-observed Greenwood inside the original `4 x 4 x 4` box, the leader in driver seat, and a positive syncer-side `isVehicleOnAllWheels` result. A Lua return value alone cannot pass it.
- Passing that gate acquires the reusable native script-camera lease, enables widescreen, installs the exact fixed camera at `(2405.9749, -1882.4752, 15.2036)`, points it at `(2405.5693, -1883.3892, 15.2158)`, and completes the native one-second fade to black before world teardown.
- CJ is removed to the SCM staging coordinate, the original Greenwood and all three protagonists are destroyed, and `LOAD_SCENE_IN_DIRECTION 2397.6443 -1880.2762 23.8686 160.0` is reproduced by the existing directional preload primitive. The black fade is preserved across camera ownership transfer into native `SWEET2B`.
- Managed file cutscenes now preserve the vanilla `SWITCH_STREAMING OFF` loading window. Native `CCutsceneMgr` still owns player safety, playback, model instances, and the matching streaming restore during postload or teardown.
- After `SWEET2B` releases, the server reconstructs the Greenwood at SCM `(2396.36, -1917.96, 12.38)` and MTA centre Z `12.88`, with heading `267.3`, plate `GROVE4L`, palette colours 59 and 34, fire-only proof, unlocked doors, and non-bursting tyres. CJ is in the driver seat. Direct passenger creation now maps the SCM slots independently to MTA seats Smoke `3`, Sweet `2`, Ryder `1`.
- The Voodoo is reconstructed at SCM `(2411.3098, -1928.8369, 12.3906)` and MTA centre Z `12.9405315`, heading `178.7106`, palette colour 22 on both channels, health `2700`, lock mode `3`, and non-bursting tyres. Its red threat blip and Ballas2 driver are present; the driver has TEC9, current-weapon selection, accuracy `40`, mission classification, and only the exact no-critical-hits flag.
- The reconstruction barrier requires all nearby vehicles and actors to be streamed and owned by the leader client and validates coordinates, headings, seats, health, and native policies for three stable samples. MTA's initial 12-bit vehicle-health field saturates the Voodoo at `2047.5`; after that streamed state is acknowledged, the server reapplies the full `2700` through the ordinary float health RPC and the route barrier observes it before assigning the driver task.
- While the screen remains black, the server creates a stable managed handle for the exact eight-point `PERFORM_SEQUENCE_TASK`. The owner epoch leases the Ballas driver and Voodoo, applies opcode `0587` as `FALSE`, and must report an active logical index before the passenger is created, preserving SCM order. Foot combat cancels the route before assigning replacement tasks. The remote Grove scene first overlaps successor leases, moves the camera streaming centre and loads collision, verifies all five scene entities for three stable samples, and only then cancels the route so the Voodoo cannot lose its native instance during handoff.
- The Ballas passenger is then created in MTA seat `1`. Its drive-by targets the Greenwood with abort range `5000`, right-side mode and frequency `60`. Ryder and Sweet target the Voodoo from the right and left with frequency `99`. All three native `TASK_SIMPLE_GANG_DRIVEBY` instances must be observed active before the one-second fade-in.
- The two Grove support actors use FAM2/FAM3 models `106/107`, the installed SCM coordinates, health `30`, TEC9s, and paired native chat tasks once both distant peds stream in. Their scalar `STAY_IN_SAME_PLACE` and `NEVER_TARGETTED` flags persist across native model recreation, so the chat task cannot pull their first remote instances together before the camera reveal.
- Voodoo destruction or health at or below `250` assigns native `TASK_COMPLEX_KILL_PED_ON_FOOT` to each surviving Ballas. Ryder and Sweet retarget the surviving driver and passenger with frequencies `70`. The same transition occurs when either Ballas dies before the Voodoo.
- The chase ends only when both Ballas are dead. CJ, Sweet, Ryder, Smoke, Greenwood, and Grove-support deaths retain their phase-specific mission-failure conditions.
- If the Greenwood reaches `250` health in an SCM-monitored driving phase, the resource plays the exact `SWE2_KA`, `SWE2_KB`, or `SWE2_KC` warning. The remaining protagonists then receive one native sequence containing opcode-equivalent immediate leave and smart flee tasks, with distance `100` and duration `10000`, before `SWE3_D` and `M_FAIL` are shown.
- If the Voodoo reaches the final `8 x 8 x 5` Grove box while both Ballas remain alive, the full alternate scene runs. It preserves the exact fixed camera, collision and directional world loads, speed `1` straight-line Voodoo task, coordinate drive-by with accuracy `50`, shoot rate `90` and frequency `100`, scripted Grove deaths at `2000` and `2500` ms, speed `30` escape, three-second wait, black teardown, camera restoration, and final `TW2_Y` failure. The syncer reports native `TASK_DIE` or sequence-progress evidence because GTA's ordinary `IsDead` query inspects a different task slot; the server then commits health zero for replication.
- Chase dialogue reproduces `SWE2_CA`, `SWE2_CC`, and `SWE2_GA..HE` with the original speakers and native mission audio. The two health-loss dialogue groups `SWE2_DA..DD` and `SWE2_EA..EE` arm after successive 60-health drops, matching the SCM ordering.
- Leaving the Greenwood during the outward drive, chase, or Grove return interrupts the active conversation and cycles the seven SCM reminders `RYDX_AF`, `SWE1_BM`, `SWEX_BP`, `RYDX_AC`, `SWEX_BS`, `RYDX_AD`, and `SWE1_BG`. After Sweet and Ryder leave, the Smoke return cycles `SMOX_AA..AE`. The car objective appears after the three-second reminder finishes, and re-entry resumes at the next conversation index.
- Once both Ballas are dead, the mission clears Ryder and Sweet's drive-by tasks and opens the exact grounded Greenwood gate at `(2513.3, -1671.9, 12.52)`. Its nonzero SCM area flag is rendered through the native important-area primitive in addition to the radar blip. The return conversation reproduces events `37651..37659`, keys `SWE2_MA..MJ`, their original speakers, and the four-second dialogue lead.
- The Grove Street drop-off starts with the exact fixed camera, switches to the original 17-second vector move and track after `SWE2_NC`, and reproduces events `37660..37667`. CJ's native look-at and the original skip window begin after `SWE2_NA`. Sweet and Ryder then receive independently observed native `leave_car` and walking sequence tasks, offset by 500 ms, before their SCM six-second departure wait expires.
- The remaining drive opens the exact grounded gate and native important-area visual at `(2066.4648, -1695.4436, 12.5547)` and reproduces events `37668..37676`. Smoke's drop-off preserves vehicle camera mode `1`, the original 17-second camera move and track, its 500 ms lead, CJ's 15-second look-at, and events `37677..37681`. Smoke's native leave-and-walk sequence must become observable before the five-second departure wait expires.
- Natural scene completion restores the camera directly. Enter, Space, and `/drivethruskip` share the leader-authorized SCM skip path: fade out for 500 ms, hold black for 500 ms while the departing actors are removed, then fade back in. The final visible result is `M_PASSS` with `$200` and mission-passed tune `1`.
- SWEET3 leaves CJ's existing weapons available during the mission. The harness still snapshots and restores the original loadout during final cleanup so weapons or ammunition acquired or consumed inside its isolated dimension do not leak into the caller's state.
- Every naturally completed mission-audio line now clears GTA's small-message queue before the next 200 ms dialogue gate. Death, destruction, and low-health checks are active only in the SCM loops that monitor them: the three protagonists through the Grove drive, Smoke alone on his final drive, Grove support only during the Ballas chase, and no broad actor or Greenwood failure during either return cutscene.

## Known gaps and pending validation

- The three SCM `DM_PED_MISSION_EMPTY` assignments have no current public primitive. Native mission-actor classification and story protection are applied, but the empty decision-maker policy remains explicit pending work.
- The first default mission-ped `m_norm.ped` adapter is implemented behind a resource-owned, syncer-only event-profile lease. It restores native `EVENT_VEHICLE_ON_FIRE` admission and response selection without changing identity during normal task execution. The 2026-07-21 integrated run confirmed leases for both Ballas, preserved normal Grove crew entry, completed the vehicle-to-foot transition, and passed the chase checkpoint. The isolated harness separately observed syncer-cycle reactivation, urgent vehicle exit, and the native smart-flee subtree; its terminal resumed-combat guard was not reached before manual cleanup.
- Cooperative support-vehicle policy is not active in this single-leader harness. `/drivethru` assigns one mission leader so the canonical Greenwood retains CJ, Ryder, Smoke, and Sweet in its four seats.
- The apparent Voodoo damage-budget mismatch still needs one controlled chase
  recording synchronized health, incoming shots and damage history.
- The Greenwood low-health branches remain pending dedicated tests.
- Native file-cutscene subtitles remain uncertain until their vanilla cue
  source and presentation path are established.
- Campaign-only respect, wanted-level reset, mission counters, progress flags, and save-stat bookkeeping remain excluded by the story handoff. The visible `$200` score award and mission-passed tune are implemented.

No SCM bytecode is executed. Lua remains the server-authoritative checkpoint orchestration while the resource consumes Neon's existing generic native cutscene, GXT, audio, actor-policy, vehicle-policy, and arrival primitives.

## Vehicle coordinate evidence

The compact GTA SA 1.0 target, SHA-256 `72ae59e44c761389e354a50dc6215e964fe771121e2f4b1877273a493ceecc9b`, implements `CCarCtrl::CreateCarForScript` at `0x431F80`. Both its automobile and boat paths call `CEntity::GetDistanceFromCentreOfMassToBaseOfModel` at `0x536BE0` and add that result to the script Z before writing the entity matrix. The current `gta-reversed-dryxio` bodies match the decisive target instructions. The installed `greenwoo.dff` embeds `greenwoo_col` as COL3 with bounding-box minimum Z `-0.5`, so SCM `CREATE_CAR ... 12.3` becomes MTA centre Z `12.8`. Passing raw `12.3` to `createVehicle` places the Greenwood half a metre too low.

The installed `voodoo.dff` embeds `voodoo_col` with bounding-box minimum Z `-0.549931526`. Its raw pursuit coordinate therefore becomes centre Z `12.9405315`. Car colour table entry 22 is RGB `(105,30,59)`, while ped model 103 is the installed `BALLAS2` entry.

The installed `main.scm`, SHA-256 `601def3baae766ce6a23e2f0b9b48f6b33c9a64e2fc32eb4f22ddea8b868b0fa`, stores the first unique route coordinate at offset `0x826A5`. The eight decoded points and speeds match the local SWEET3 source exactly. The route uses model `412`, normal mode, and `DRIVINGMODE_AVOIDCARS`; it is consumed through the already audited generic `drive_to` sequence descriptor.

MTA's `SVehicleHealthSync` stores vehicle health in 12 bits at `0.5` precision, giving an explicit maximum of `2047.5`. The entity-add packet uses that compact field, while the later `SET_ELEMENT_HEALTH` RPC writes a full float and `CClientVehicle::SetHealth` forwards it to the native vehicle. The post-stream rearm preserves SWEET3's `2700` locally without changing the network protocol or adding a mission-specific C++ primitive.

## Native property evidence

`SET_CAR_PROOFS` opcode `02AC` enters the target handler at `0x47F908`, resolves the vehicle, then writes the five independent physical flags through the shared block at `0x47F8AA`. In script argument order these are bullet `0x40000`, fire `0x80000`, explosion `0x800000`, collision `0x100000`, and melee `0x200000` in `CPhysical+0x40`. The current `gta-reversed-dryxio` handler matches those writes. `setVehiclePhysicalProofs` persists the exact tuple across local native vehicle recreation instead of using MTA's broader damage-proof switch.

`SET_CHAR_SUFFERS_CRITICAL_HITS` remains the previously verified inverse write at `0x48A023` to `CPed+0x470` bit `0x1000`. `setPedSuffersCriticalHits` exposes that one flag independently so the enemy driver does not inherit the protagonists' unrelated target and vehicle-jacking protections.

`SWITCH_STREAMING OFF` reaches `0x48478E`; its decisive write at `0x4847A1` stores the inverse script argument in global `0x9654B0`. Native cutscene postload and deletion restore false at `0x5AFBE8` and `0x5AFF90`. The managed file-cutscene path now sets the same flag immediately before native load and retains native restoration.

## Validation evidence

The complete first checkpoint was validated in game on 20 July 2026. `SWEET2A` finished naturally after `31453 ms` without a crash or model `304` load dialog. Client and server both observed the Greenwood at `(2508.700, -1671.700, 12.800)` with RGB colours `(78,104,129)/(100,100,100)` and plate `GROVE4L`. The streaming barrier accepted all three native entry tasks, the server confirmed Ryder, Smoke, and Sweet in MTA seats `1`, `2`, and `3`, and `SWE2_AA` completed before the leader entered as driver. The terminal `09D0` gate passed at `(2407.68, -1888.30, 13.16)` with the leader driving and GTA reporting all four wheels in contact. `/drivethruabort` then restored the mission state cleanly. No Drive-Thru client or server error was logged during the run.

Native `SWEET2B` was validated through its natural completion after `83313 ms`. Its first reconstruction exposed MTA's entity-add health saturation: the streamed Voodoo arrived at the documented `2047.5` network maximum instead of SWEET3's `2700`, so the exact-health barrier correctly refused progression. The corrected path acknowledges that compact initial value, reapplies `2700` through the full-float health RPC, and requires the leader client to observe the vanilla value before route assignment. Two following runs passed reconstruction in less than one second without remaining on the black screen.

The integrated chase was validated twice in game on 20 July 2026. Both runs observed the driver's native route before creating the passenger, then observed all three native drive-by tasks before the visible fade-in. Real fire reduced the Voodoo to the vanilla `250` threshold, both surviving Ballas accepted native `TASK_COMPLEX_KILL_PED_ON_FOOT`, and the checkpoint passed only after both were dead. The server recorded the two foot-combat transitions at `14:18:20` and `14:19:54`, followed by terminal passes at `14:18:30` and `14:20:06`. The tester observed one Ballas engage CJ while the other left the vehicle but died in its explosion, which is compatible with GTA's kill-on-foot task beginning with its own leave-car subtask. No Drive-Thru error was logged in either chase run.

The final return slice and the four Lua parity corrections were validated on the complete happy path on 20 July 2026. The first Grove alternate-failure runs exposed a native-task guard timeout, shots continuing after the scripted deaths, weapon aim following the local camera, and finally route completion restoring the driver's automatic syncer during the remote-scene barrier. The task lifetime, remaining native AI `IsPlayer` branches and two-phase route-to-camera ownership handoff were corrected. On 21 July the final `/drivethrusimfar` regression advanced through all eight logical route points, committed driver and Voodoo ownership before camera preparation, validated all five scene entities for three stable samples, activated the slow drive and coordinate drive-by, observed both scripted deaths, started the escape and restored the camera before the expected `TW2_Y` failure. The Greenwood low-health branches have not yet received their dedicated tests. Those tests should separately damage the Greenwood to `250` health during the outward drive, chase, Grove return, and Smoke return. `/drivethrutracechat on` exposes the warning, flee, alternate camera, drive-by, death and restoration gates without influencing them.

The isolated simulation-island harness was validated in game on 21 July 2026. With opcode `0587` applied and resource-owned ped and vehicle streaming leases active, both persistent syncers remained assigned more than `3.3 km` from the player. The Voodoo advanced through native sequence indices `1`, `2`, and `3`, synchronized `259.47 m` of horizontal movement with `groundValid=true` and a stable road-height Z near `13.2`, then continued to index `4` after `/nativesimnear` returned the player beside its current position. The later integrated Drive-Thru regression validated the same foundation through the complete eight-point route.

The separate task-handoff harness was validated on 21 July 2026. It revoked epoch `1` at logical index `3` while far away, observed native stream-out for both elements, reconstructed epoch `2` at the same index with `0.00 m` discontinuity, and synchronized another `22.2 m` at Z `13.21` before cleanup observed index `5`. Drive-Thru consumes that stable route layer and its integrated `/drivethrusimfar` regression additionally proved the successor-scene lease overlap and persistent ownership commit at the terminal route boundary.
